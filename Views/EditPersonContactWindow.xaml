<Window x:Class="ExampleMvvmPattern.Views.EditPersonContactWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExampleMvvmPattern.Views"
        xmlns:vm="clr-namespace:ExampleMvvmPattern.ViewModels"
        mc:Ignorable="d"
        Title="Person Contact" Height="450" Width="500"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Margin="5"
                   VerticalAlignment="Center"
                   Text="Name:" />
        <TextBox Grid.Row="0" 
                 Grid.Column="1"
                 VerticalAlignment="Center"
                 Margin="5"
                 Text="{Binding CurrentContact.Name, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="1" 
                   Grid.Column="0"
                   Margin="5"
                   VerticalAlignment="Center"
                   Text="Surname:" />
        <TextBox Grid.Row="1" 
                 Grid.Column="1"
                 VerticalAlignment="Center"
                 Margin="5"
                 Text="{Binding CurrentContact.Surname, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="2" 
                   Grid.Column="0"
                   Margin="5"
                   VerticalAlignment="Center"
                   Text="Email:" />
        <TextBox Grid.Row="2" 
                 Grid.Column="1"
                 VerticalAlignment="Center"
                 Margin="5"
                 Text="{Binding CurrentContact.Email, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="3" 
                   Grid.Column="0"
                   Margin="5"
                   VerticalAlignment="Center"
                   Text="Location:" />
        <TextBox Grid.Row="3" 
                 Grid.Column="1"
                 VerticalAlignment="Center"
                 Margin="5"
                 Text="{Binding CurrentContact.Location, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="4" 
                   Grid.Column="0"
                   Margin="5"
                   VerticalAlignment="Center"
                   Text="Phone Number:" />
        <TextBox Grid.Row="4" 
                 Grid.Column="1"
                 VerticalAlignment="Center"
                 Margin="5"
                 Text="{Binding CurrentContact.PhoneNumber, UpdateSourceTrigger=PropertyChanged}"/>

        <StackPanel Grid.Row="5" Grid.Column="2" Margin="10" HorizontalAlignment="Right" VerticalAlignment="Bottom" Orientation="Horizontal">
            <Button Content="Save" Width="75" Margin="5" Command="{Binding SaveCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" />
            <Button Content="Cancel" Width="75" Margin="5" Command="{Binding CancelCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" />
        </StackPanel>
    </Grid>
</Window>
